# Dipendenze runtime PINNATE per build riproducibili (audit A7).
# Le versioni sono il minimo già dichiarato e testato del progetto: scelta
# conservativa e a rischio zero. Si possono alzare a un patch più recente DOPO
# aver verificato la build su Windows (la GUI/Telegram non girano in CI).
customtkinter==5.2.0
python-telegram-bot==20.0
